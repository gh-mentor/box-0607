# This script uses git commands to stage changes, commit the changes, and push the changes to the main branch remote repository.

# Stage all changes
git add .

# Commit changes
git commit -m "Update"

# Push changes to main branch remote repository
git push origin main